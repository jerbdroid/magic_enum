licenses(["notice"])

package(default_visibility = ["//visibility:public"])

exports_files(["LICENSE"])

cc_library(
    name = "magic_enum",
    hdrs = glob(["include/magic_enum/*.hpp"]),
    defines = ["MAGIC_ENUM_NO_EXCEPTION"],
    includes = ["include"],
)
